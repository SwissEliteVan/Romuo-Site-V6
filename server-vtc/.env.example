# ============================================================================
# ROMUO VTC - CONFIGURATION SERVEUR
# ============================================================================

# Node Environment
NODE_ENV=development

# Server
PORT=4000
HOST=0.0.0.0

# Database
# PostgreSQL (Production)
DATABASE_URL="postgresql://user:password@localhost:5432/romuo_vtc?schema=public"

# SQLite (MVP/Dev simplifié)
# DATABASE_URL="file:./dev.db"

# JWT Secret (Générez une clé forte en production !)
JWT_SECRET=votre-secret-jwt-super-securise-changez-moi
JWT_REFRESH_SECRET=votre-secret-refresh-jwt-super-securise-changez-moi

# CORS (URLs des frontends autorisées, séparées par des virgules)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# Frontend URLs (pour les frontends)
RIDER_APP_URL=http://localhost:3000
DRIVER_APP_URL=http://localhost:3001
ADMIN_APP_URL=http://localhost:3002

# WebSocket
WS_PORT=4000

# Logging
LOG_LEVEL=info
# Options: error, warn, info, debug

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Maps API (optionnel, pour géocodage plus précis)
# GOOGLE_MAPS_API_KEY=your_api_key_here
# MAPBOX_API_KEY=your_api_key_here

# Payment (Phase 2)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLIC_KEY=pk_test_...

# Email (Phase 2)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_password
